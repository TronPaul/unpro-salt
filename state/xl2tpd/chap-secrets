{%- set server = salt['pillar.get']('xl2tpd:lns', {}) -%}
{%- set server_name = server.get('name', 'l2tp') -%}
{%- for secret in server.get('secrets', []) -%}
{%- if 'ip_address' in secret -%}
{{ secret['username'] }} {{ server_name }} password {{ secret['ip_address'] }}
{%- else -%}
{{ secret['username'] }} {{ server_name }} password *
{%- endif %}
{% endfor -%}
