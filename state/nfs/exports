/export {{ ' '.join(["{subnet}({options})".format(subnet=subnet, options=options) for subnet, options in mount_conf.get('nfs:root:subnets', {}).items()]) }}
{% for name, mount_conf in salt['pillar.get']('nfs:mounts', {}).items() %}
/export/{{ name }} {{ ' '.join(["{subnet}({options})".format(subnet=subnet, options=options) for subnet, options in mount_conf.get('subnets', {}).items()]) }}
{% endfor %}
